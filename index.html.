<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Trailstronauts</title>
  <style>
    /* same styles as above (paste from Option A) */
  </style>
</head>
<body>
  <div class="stars" aria-hidden="true"></div>
  <div class="ring" aria-hidden="true"></div>
  <main class="card" role="main" aria-live="polite">
    <h1 id="msg">Loading…</h1>
    <p>Thanks for scanning — have a beautiful Burn. ✶</p>
  </main>
  <footer>Trailstronauts • Tomorrow Today</footer>

  <script>
    const MESSAGES = [
      "You belong outside — and out there.",
      "Find your people. Or be your people.",
      "Take up space. Share the trail.",
      "You are the wonder you’re chasing.",
      "Dusty, loved, and perfectly enough.",
      "Joy is a direction, not a place.",
      "Breathe. Look up. Keep wandering."
    ];
    const mode = "daily"; // "daily" or "random"
    let idx = 0;
    if (mode === "daily") {
      const d = new Date();
      const seed = d.getUTCFullYear()*372 + (d.getUTCMonth()+1)*31 + d.getUTCDate();
      idx = seed % MESSAGES.length;
    } else {
      idx = Math.floor(Math.random() * MESSAGES.length);
    }
    document.getElementById("msg").textContent = MESSAGES[idx];
  </script>
</body>
</html>
